# Toxic-only WordCloud

Cleaning is display-only: lowercase, URL/mention removal, hashtag-content preservation, punctuation/symbol removal, and Indonesian stopword removal. Raw data is unchanged.

`rows` keeps duplicate-row weighting. `unique` counts each exact text once as a sensitivity view.
